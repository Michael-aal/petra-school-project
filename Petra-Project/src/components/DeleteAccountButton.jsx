export default function DeleteAccountButton() {
  // Account deletion is intentionally owned by the Settings page only.
  // This legacy component remains as a compatibility stub for older imports.
  return null;
}
